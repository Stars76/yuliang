# Android 正式签名配置模板。
# 使用方法：复制本文件为 keystore.properties（已被 android/.gitignore 忽略，绝不会入库），填入你自己的值。
#
# 生成密钥库（建议）：在 src/standalone/capacitor/android/ 下执行
#   keytool -genkeypair -v -keystore yuliang-release.jks -keyalg RSA -keysize 2048 -validity 36500 -alias yuliang
# 然后把 storeFile 指向该文件（路径相对 android 根，或写绝对路径）。
#
# 注意：keytool 提示的「姓名/组织/城市/国家」只是证书签名者标识，可填昵称或随意标签（如 yuliang），
# Android 不校验是否真人。它只出现在 APK 签名证书里，不会公开上网；你的密码/密钥永不入库。
#
# 警告：keystore 文件（*.jks）与 keystore.properties 都包含秘密，切勿提交到仓库。
storeFile=yuliang-release.jks
storePassword=CHANGE_ME_store_password
keyAlias=yuliang
keyPassword=CHANGE_ME_key_password
